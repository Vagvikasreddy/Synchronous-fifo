# Synchronous-fifo
A synchronous FIFO designed in Verilog HDL with configurable data width and depth. The design supports write/read operations, full and empty status flags, pointer-based memory management, reset functionality, and error detection for invalid read/write attempts. Includes a testbench for functional simulation and verification.
